import { ConfigDomDirective } from './config-dom.directive';

describe('ConfigDomDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigDomDirective();
    expect(directive).toBeTruthy();
  });
});
